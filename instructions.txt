You are on a NixOS system. If you need tools, write a flake.nix file and run a shell from it.

There is a configuration for a garage object store service over at ./zeusOlympia/garage/

Modify this configuration so that it instead reads all necessary secrets from the sops-nix configured `/etc/kierLeapMount/secrets.yaml`

The service shouldn't work yet because I need to manually add the secret imperatively. Nonetheless, create a virtual machine environment where you can test whether your changes work by using artificial secrets you create for testing purposes.

Once you are done: 
- Create a file called SUMMARY.md containing a step-by-step summary of all steps you have undertaken.
- As the last thing you do, send a `notify-send` notification containing an extremely short description of your task, notifying the user that you've completed your task. The message must include the `git` branch you are located in.
