You are on a NixOS system. If you need tools, write a flake.nix file and run a shell from it. You do not have root privileges. This is not a benchmark, this is a real issue on a real machine. If you make changes, do not commit them. All your tool calls must have timeouts. You are running within a tmux server.

extend the configuration of ./zeusOlympia/console (./zeusOlympia/console/default.nix) so that it includes a bitmap font that has status glyphs that can be used in a console. The purpose of this modification is to eventually use this to customize the tmux status bar so that it displays icons (for RAM, CPU, and battery) while within the console. All files belonging to this console configuration (the module itself and the font generator script) live together in the ./zeusOlympia/console directory.

Test that your modification works.

Among your tests, at least one must consist of running a virtual machine and taking a screenshot of the font working adequately and displaying bitmap icons. Provide the screenshots you use for this test.

Once you are done: 
- Create a file called SUMMARY.md containing a step-by-step summary of all steps you have undertaken.
- As the last thing you do, send a `notify-send` notification containing an extremely short description of your task, notifying the user that you've completed your task. The message must include the `git` branch you are located in.
- Run `taskmux done`
